<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Course Feedback</title>
    <style>
        /* Basic Reset & Body Styling */
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Arial', sans-serif;
            background: #f5f5f5; /* Background color for the body */
            color: #333;
            line-height: 1.6;
            padding: 20px;
        }

        /* Form Container Styling */
        .feedback-form-container {
            max-width: 600px;
            margin: 40px auto;
            padding: 30px;
            background-color: #ffffff;
            border-radius: 10px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }

        /* Form Heading with custom header color */
        .feedback-form-container h2 {
            text-align: center;
            color: #3498db; /* Blue color for the header */
            margin-bottom: 20px;
            font-size: 1.5rem;
        }

        /* Optional intro text */
        .feedback-form-container p {
            text-align: center;
            color: #555;
            margin-bottom: 30px;
            font-size: 1rem;
        }

        /* Form Group Styling (for spacing) */
        .form-group {
            margin-bottom: 20px;
        }

        /* Label Styling */
        .form-group label {
            display: block;
            font-weight: 600;
            margin-bottom: 8px;
            color: #34495e;
        }

        /* Input Field & Text Area Styling */
        .form-group input[type="text"],
        .form-group input[type="email"],
        .form-group textarea {
            width: 100%;
            padding: 12px 15px;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-size: 1rem;
            color: #333;
            transition: border-color 0.3s ease, box-shadow 0.3s ease;
        }

        /* Input Field & Text Area Hover/Focus States */
        .form-group input[type="text"]:hover,
        .form-group input[type="email"]:hover,
        .form-group textarea:hover {
            border-color: #88bce8;
        }

        .form-group input[type="text"]:focus,
        .form-group input[type="email"]:focus,
        .form-group textarea:focus {
            border-color: #5fa8d3;
            box-shadow: 0 0 5px rgba(95, 168, 211, 0.5);
            outline: none;
        }

        /* Rating Group Styling */
        .rating-group {
            display: flex;
            justify-content: space-around;
            align-items: center;
            padding: 10px 0;
            border: 1px solid #eee;
            border-radius: 5px;
            background-color: #fdfdfd;
        }

        .rating-group label {
            display: flex;
            flex-direction: column;
            align-items: center;
            cursor: pointer;
            font-weight: normal;
            margin-bottom: 0;
            color: #555;
            transition: color 0.3s ease, transform 0.2s ease;
            padding: 5px;
        }

        .rating-group input[type="radio"] {
            margin-bottom: 5px;
            cursor: pointer;
        }

        .rating-group input[type="radio"]:checked + span {
             font-weight: bold;
             color: #367cbf;
        }

        .rating-group label:hover {
            color: #367cbf;
            transform: scale(1.1);
        }

        /* Submit Button Styling */
        .submit-btn {
            display: block;
            width: 100%;
            padding: 12px 20px;
            background-color: #5cb85c;
            color: #ffffff;
            border: none;
            border-radius: 5px;
            font-size: 1.1rem;
            font-weight: bold;
            cursor: pointer;
            text-align: center;
            transition: background-color 0.3s ease, transform 0.1s ease;
            margin-top: 10px;
        }

        .submit-btn:hover {
            background-color: #4cae4c;
        }

        .submit-btn:active {
             background-color: #449d44;
             transform: translateY(1px);
        }

        /* Success/Failure Message Styling */
        .message {
            text-align: center;
            font-size: 1.1rem;
            margin-top: 20px;
            padding: 10px;
            border-radius: 5px;
            display: none; /* Hidden by default */
        }

        .message.success {
            background-color: #d4edda;
            color: #155724;
        }

        .message.error {
            background-color: #f8d7da;
            color: #721c24;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .feedback-form-container {
                margin: 20px auto;
                padding: 20px;
            }

            .feedback-form-container h2 {
                 font-size: 1.3rem;
            }
        }

    </style>
</head>
<body>

    <div class="feedback-form-container">
        <h2>Course Feedback</h2>
        <p>We appreciate you taking the time to share your thoughts about the course!</p>

        <form id="courseFeedbackForm" action="#" method="POST">

            <div class="form-group">
                <label for="userName">Name <span style="color: red;">*</span></label>
                <input type="text" id="userName" name="userName" placeholder="Enter your name" required>
            </div>

            <div class="form-group">
                <label for="userEmail">Email <span style="color: red;">*</span></label>
                <input type="email" id="userEmail" name="userEmail" placeholder="Enter your email address" required>
            </div>

            <div class="form-group">
                <label>Overall Rating <span style="color: red;">*</span></label>
                <div class="rating-group" role="radiogroup" aria-labelledby="rating-label">
                    <label>
                        <input type="radio" id="rating1" name="rating" value="1" required>
                        <span>1</span>
                    </label>
                    <label>
                        <input type="radio" id="rating2" name="rating" value="2" required>
                        <span>2</span>
                    </label>
                    <label>
                        <input type="radio" id="rating3" name="rating" value="3" required>
                        <span>3</span>
                    </label>
                    <label>
                        <input type="radio" id="rating4" name="rating" value="4" required>
                        <span>4</span>
                    </label>
                    <label>
                        <input type="radio" id="rating5" name="rating" value="5" required>
                        <span>5</span>
                    </label>
                </div>
            </div>

            <div class="form-group">
                <label for="comments">Additional Comments <span style="color: red;">*</span></label>
                <textarea id="comments" name="comments" rows="4" placeholder="Share your thoughts" required></textarea>
            </div>

            <button type="submit" class="submit-btn">Submit Feedback</button>

        </form>

        <!-- Success/Failure Message -->
        <div id="message" class="message"></div>
    </div>

    <script>
        // Handle form submission with success or error message
        document.getElementById('courseFeedbackForm').addEventListener('submit', function(event) {
            event.preventDefault();  // Prevent form from submitting normally

            // Simulate form submission success
            var messageElement = document.getElementById('message');
            messageElement.className = 'message success';
            messageElement.innerHTML = 'Thank you for your feedback! Your response has been recorded.';
            messageElement.style.display = 'block';

            // Optionally clear form fields after submission
            document.getElementById('courseFeedbackForm').reset();
        });
    </script>

</body>
</html>
